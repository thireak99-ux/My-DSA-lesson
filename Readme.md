## Note

DSA

*Data structure* = is a way of storing and orgainizing data in order to use it effectively and efficiently.

data structure we have 2 types is

linear and non linear


*Algorithm* = a step or method for solving specific coding problem.

Program = Data structure + Algorithm 

* if in every programming language have variable why do some programming write like this *


data_type name = value;

cuz we have statically-type and dynamically-type 

statically-type is a type that make us error alot ex. cpp , java 

** the advantage of using statically-type is that we write the code without worried about the type where we called it a type-safety. 


dynamically-type is make us easy to write and read the code but the worst thing is about the type of it. ex. java-script , python. 

so when we write the dynamic programming language we have to double check the type of it when we calculate


variable we have name , type , value and when we declare it without adding value in it catch a garbage value cuz a declare variable is where we go and catch a space in memory location so this is about CPP.

WE learn here is where we want to understand the use case of it so that we understand where or when we should use it 


two way of DATA STRUCTURE:

Linear : store elements in sequential order ( oen after one to another )

Non-Linear : store elements in many direction such as ( heirarchy and tree and network(graph)) 


ALGORITHM : 

        Input > Process > Output

What make a good Algorithm ?

    - Correctness 
    - Efficientcy:
        - Time : take time to write and run the code
        - Space : how much memory it takes to execute the operation 

Growth rate : is the way of how it changes បម្រែបម្រួលពីតូចទៅធំ 

Asymptotic Notation is way of how we measure the algorithm of Time complexcity and Space complexcity 

we measure with Best case (Omega), average case (theta), worst case (Big O). 

*** RECAP of we write up there ***

- Data structure & Algorithm
- Growth Rate
- Asymptotic Notation # data-structure
# data-structure
# data-structure
# data-structure
# data-structure
# data-structure
# data-structure
